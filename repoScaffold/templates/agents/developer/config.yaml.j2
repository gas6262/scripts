name: developer
description: Builds features, fixes bugs, and addresses PR feedback from the Linear board.
sleep_seconds: {{ agent.sleep_seconds | default(600) }}
max_turns: {{ agent.max_turns | default(1000) }}
